
version: 2
jobs:
  build:
    working_directory: /coolcounterapp
    docker:
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: install-angular
          command: 'sudo npm install -g angular@7.3.0'
      - run:
          name: ng-lint
          command: 'ng lint'